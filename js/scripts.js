
function drawTree(p1) {

  for ( var i = 0 ; i < p1 ; i++) {
    var star = '';
      for( var j = 0 ; j < p1 ; j++) {
        star += '*';
            console.log(star);
      }
  }
}
var x = drawTree(6);
